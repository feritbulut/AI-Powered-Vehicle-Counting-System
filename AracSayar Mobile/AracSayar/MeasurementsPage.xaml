<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AracSayar.MeasurementsPage"
             Title="Ölçümler"
             BackgroundColor="#F5F7FA">

    <Grid RowDefinitions="Auto,*" 
          Padding="20,10,20,20"
          RowSpacing="15">

        <Border Grid.Row="0"
                BackgroundColor="#2C3E50"
                Padding="20,16"
                Margin="0,10,0,0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="16"/>
            </Border.StrokeShape>
            <Border.Shadow>
                <Shadow Brush="#40000000"
                        Offset="0,4"
                        Radius="12"
                        Opacity="0.3"/>
            </Border.Shadow>

            <HorizontalStackLayout Spacing="12"
                                   HorizontalOptions="Center">
                <Label Text="📈"
                       FontSize="24"
                       VerticalOptions="Center"/>
                <Label Text="Geçmiş Ölçümler"
                       TextColor="White"
                       FontSize="20"
                       FontAttributes="Bold"
                       VerticalOptions="Center"/>
            </HorizontalStackLayout>
        </Border>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding}"
                        SelectionMode="None">

            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center"
                                     HorizontalOptions="Center"
                                     Spacing="20"
                                     Padding="40">
                    <Label Text="📭"
                           FontSize="64"
                           HorizontalOptions="Center"
                           Opacity="0.5"/>
                    <Label Text="Henüz ölçüm yok"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#7F8C8D"
                           HorizontalOptions="Center"/>
                    <Label Text="İlk ölçümünüzü yapmak için ana sayfaya dönün"
                           FontSize="14"
                           TextColor="#95A5A6"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="WordWrap"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView Margin="0,8">
                        <SwipeView.RightItems>
                            <SwipeItems Mode="Reveal">
                                <SwipeItem Text="   Sil"
                                           BackgroundColor="#E74C3C"
                                           Invoked="OnDeleteInvoked">
                                    <SwipeItem.IconImageSource>
                                        <FontImageSource Glyph="🗑️"
                                                        FontFamily="Arial"
                                                        Size="24"
                                                        Color="White"/>
                                    </SwipeItem.IconImageSource>
                                </SwipeItem>
                            </SwipeItems>
                        </SwipeView.RightItems>

                        <Border BackgroundColor="White"
                                Padding="0"
                                StrokeThickness="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="16"/>
                            </Border.StrokeShape>
                            <Border.Shadow>
                                <Shadow Brush="#20000000"
                                        Offset="0,2"
                                        Radius="8"
                                        Opacity="0.15"/>
                            </Border.Shadow>

                            <Grid ColumnDefinitions="8,*"
                                  ColumnSpacing="0">

                                <BoxView Grid.Column="0"
                                         BackgroundColor="#3498DB"
                                         WidthRequest="8">  
                                    <BoxView.Clip>
                                        <RectangleGeometry Rect="0,0,8,100"/>
                                    </BoxView.Clip>
                                </BoxView>


                                <VerticalStackLayout Grid.Column="1"
                                                     Padding="20,16"
                                                     Spacing="12">

                                    <HorizontalStackLayout Spacing="8">
                                        <Label Text="📅"
                                               FontSize="18"
                                               VerticalOptions="Center"/>
                                        <Label Text="{Binding Date, StringFormat='{0:dd MMMM yyyy}'}"
                                               FontSize="18"
                                               FontAttributes="Bold"
                                               TextColor="#2C3E50"
                                               VerticalOptions="Center"/>
                                    </HorizontalStackLayout>

                                    <BoxView HeightRequest="1"
                                             BackgroundColor="#ECF0F1"
                                             Margin="0,4"/>

                                    <Grid ColumnDefinitions="*,*"
                                          RowDefinitions="Auto,Auto"
                                          ColumnSpacing="16"
                                          RowSpacing="10">

                                        <VerticalStackLayout Grid.Row="0" Grid.Column="0" Spacing="4">
                                            <Label Text="🕐 Başlangıç"
                                                   FontSize="12"
                                                   TextColor="#95A5A6"
                                                   FontAttributes="Bold"/>
                                            <Label Text="{Binding StartTime, StringFormat='{0:HH:mm:ss}'}"
                                                   FontSize="15"
                                                   TextColor="#34495E"
                                                   FontAttributes="Bold"/>
                                        </VerticalStackLayout>

                                        <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="4">
                                            <Label Text="🕐 Bitiş"
                                                   FontSize="12"
                                                   TextColor="#95A5A6"
                                                   FontAttributes="Bold"/>
                                            <Label Text="{Binding EndTime, StringFormat='{0:HH:mm:ss}'}"
                                                   FontSize="15"
                                                   TextColor="#34495E"
                                                   FontAttributes="Bold"/>
                                        </VerticalStackLayout>

                                        <VerticalStackLayout Grid.Row="1" Grid.Column="0" Spacing="4">
                                            <Label Text="⏱️ Süre"
                                                   FontSize="12"
                                                   TextColor="#95A5A6"
                                                   FontAttributes="Bold"/>
                                            <Label Text="{Binding DurationMinutes, StringFormat='{0:F2} dk'}"
                                                   FontSize="15"
                                                   TextColor="#E67E22"
                                                   FontAttributes="Bold"/>
                                        </VerticalStackLayout>

                                        <VerticalStackLayout Grid.Row="1" Grid.Column="1" Spacing="4">
                                            <Label Text="🚗 Araç Sayısı"
                                                   FontSize="12"
                                                   TextColor="#95A5A6"
                                                   FontAttributes="Bold"/>
                                            <Label Text="{Binding VehicleCount}"
                                                   FontSize="15"
                                                   TextColor="#27AE60"
                                                   FontAttributes="Bold"/>
                                        </VerticalStackLayout>

                                    </Grid>

                                    <Label Text="← Kaydırarak sil  "
                                           FontSize="11"
                                           TextColor="#BDC3C7"
                                           HorizontalOptions="End"
                                           Margin="0,8,0,0"
                                           FontAttributes="Italic"/>

                                </VerticalStackLayout>
                            </Grid>
                        </Border>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>